android {
    namespace "com.example.supporters_timecheck"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.supporters_timecheck"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
